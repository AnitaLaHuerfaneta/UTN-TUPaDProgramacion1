#1) Crear un programa que imprima por pantalla el mensaje: “Hola Mundo!”
print("Hola Mundo!")

#2) Crear un programa que pida al usuario su nombre e imprima por pantalla un saludo usando
# el nombre ingresado. Por ejemplo: si el usuario ingresa “Marcos”, el programa debe imprimir
# por pantalla “Hola Marcos!”. Consejo: esto será más sencillo si utilizas print(f…) para
# realizar la impresión por pantalla

nombre = input("Por favor ingrese su nombre: ")
print(f"Hola {nombre}!")

#3) Crear un programa que pida al usuario su nombre, apellido, edad y lugar de residencia e
# imprima por pantalla una oración con los datos ingresados. Por ejemplo: si el usuario ingresa
# “Marcos”, “Pérez”, “30” y “Argentina”, el programa debe imprimir “Soy Marcos Pérez, tengo 30
# años y vivo en Argentina”. Consejo: esto será más sencillo si utilizas print(f…) para realizar
# la impresión por pantalla.

nombre = input("Ingrese su nombre: ")
apellido = input("Ingrese su apellido: ")
edad = input("Ingrese su edad: ")
lugar = input("Ingrese su lugar de residencia: ")
print(f"Soy {nombre} {apellido}, tengo {edad} años y vivo en {lugar}")


#4) Crear un programa que pida al usuario el radio de un círculo e imprima por pantalla su área y
# su perímetro.

radio = input("Ingrese el radio del circulo: ")
radio = float(radio)
pi = 3.14 
area = pi * (radio ** 2)
perimetro = 2 * pi * radio
print(f"El area es: {area} y el perimetro es {perimetro}")

#5) Crear un programa que pida al usuario una cantidad de segundos e imprima por pantalla a
# cuántas horas equivale.
tiempoSeg = input("Ingrese una cantidad de segundos y le dire a cuantas horas equivale: ")
tiempoSeg = int(tiempoSeg)
tiempoMin = tiempoSeg / 60
tiempoHora = tiempoMin / 60
print(f"Los segundos ingresados equivalen a {tiempoHora} Horas")

#6) Crear un programa que pida al usuario un número e imprima por pantalla la tabla de
#multiplicar de dicho número.
numero1 = int(input("Ingrese un numero y le dire la tabla de multiplicar: "))
print(f"{numero1*1}, {numero1*2}, {numero1*3}, {numero1*4}, {numero1*5}, {numero1*6}, {numero1*7}, {numero1*8}, {numero1*9}, {numero1*10}")
#Se que se puede hacer de otras maneras (ej con paso en pseint) pero como no llegamos ahi en el modulo esperare a tener el material.

#7) Crear un programa que pida al usuario dos números enteros distintos del 0 y muestre por
#pantalla el resultado de sumarlos, dividirlos, multiplicarlos y restarlos.
num1 = float(input("Por favor ingrese el primer numero (dinstinto a 0)"))
num2 = float(input("Por favor ingrese el segundo numero (distinto a 0)"))
sumaNum = num1 + num2
restaNum = num1 - num2
multNum = num1 * num2
divNum = round(num1 / num2, 2)
print(f"""
      La suma de los numeros es: {sumaNum}
      La resta de los numeros es: {restaNum}
      La multiplicacion de los numeros es: {multNum}
      La division de los numeros es : {divNum}
        """)



#8) Crear un programa que pida al usuario su altura y su peso e imprima por pantalla su índice
#de masa corporal.
altura = float(input("Por favor ingrese su altura en metros: "))
peso = float(input("Por favor ingrese su pero en kg: "))
imc = peso / (altura*altura)
print(f"Tu indice de masa corporal es {imc}")

#9) Crear un programa que pida al usuario una temperatura en grados Celsius e imprima por
#pantalla su equivalente en grados Fahrenheit. 
celsius = float(input("Ingrese la temperatura en celsius: "))
fahr = (celsius * 9/5) + 32
print(f"{celsius}C es equivalente a {fahr}F")

#10) Crear un programa que pida al usuario 3 números e imprima por pantalla el promedio de
#dichos números.
num1 = float(input("Ingrese el primer numero:"))
num2 = float(input("Ingrese el segundo numero: "))
num3 = float(input("Ingrese el tercer numero: "))
promedio = num1 + num2 + num3 / 3
print(f"El promedio de los 3 numeros es {promedio: .2f}")
#Tuve que buscar online como hacer para mostrar solamente 2 nros decimales y evitar un nro decimal de muchos digitos (tambien aplicable en ejercicios anteriores)
